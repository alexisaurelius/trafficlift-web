Replace audit JSON upload with three text-section uploads

Admin uploads now consist of three markdown blocks (On-Page SEO,
Technical and Performance, Authority) using **Item:** entries with
Current state / Analysis / Status lines. The customer dashboard parses
those blocks and shows them under a left-rail filter (All Suggestions,
On-Page, Technical and Performance, Authority) with bold labels for each
field.

- Add onPageContent, techPerfContent, authorityContent to Audit + migration
- Rewrite admin-audits-panel to three textareas, drop manual checks editor
- Add audit-text-sections lib (parser, statuses, examples)
- Replace audit details and shared-report pages with AuditSectionsPanel
- Drop unused legacy components (audit-topic-panel, audit-check-results,
  report-markdown-panel) and helper libs (admin-audit-upload, audit-check-order)
- Update PATCH/upload/spec API routes for the new payload
